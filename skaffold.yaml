apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/endocode/kelefstis:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/endocode/kelefstis:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: kelefstis
        chartPath: charts/kelefstis
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/endocode/kelefstis'
          image.tag: '{{.DIGEST_HEX}}'
