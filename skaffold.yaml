apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/thomasfricke/kelefstis:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/thomasfricke/kelefstis:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: kelefstis
        chartPath: charts/kelefstis
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/thomasfricke/kelefstis'
          image.tag: '{{.DIGEST_HEX}}'
